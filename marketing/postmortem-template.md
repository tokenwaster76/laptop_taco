# Launch postmortem — Laptop Taco 🌮

**Launch date:** `<YYYY-MM-DD>`
**Author:** `<you>`
**Time-to-write:** within 48 hours of launch (longer than that and the details rot)

## 1. What we launched

`<one paragraph: the version, the surfaces it went out on, the headline framing>`

## 2. Where we posted

Channel-by-channel summary. Include exact URLs.

- [ ] Hacker News — `<link>` — `<rank reached, comment count>`
- [ ] Lobsters — `<link or "did not post and why">`
- [ ] Product Hunt — `<link or "did not launch and why">`
- [ ] Reddit — `<sub, link, upvote ratio>`
- [ ] Indie Hackers — `<link>`
- [ ] X / Twitter — `<thread link, top tweet impressions>`
- [ ] Bluesky — `<post link>`
- [ ] Mastodon — `<post link>`
- [ ] LinkedIn — `<post link>`
- [ ] Threads — `<post link>`
- [ ] DEV.to — `<article link, status>`
- [ ] Hashnode — `<article link, status>`
- [ ] Discord — `<which server>`
- [ ] Slack — `<which workspace>`

## 3. Traffic / stars

- GitHub stars before launch: `<n>`
- GitHub stars 24h after: `<n>`
- GitHub stars 48h after: `<n>`
- Repo clones (Insights → Traffic): `<n>`
- Unique visitors: `<n>`
- Top referrers: `<list>`
- New issues opened: `<n>` (link the gold ones)
- New PRs opened: `<n>`

## 4. Best comments / quotes

Capture screenshots before they get edited/deleted.

- `<quote 1, source, link>`
- `<quote 2, source, link>`
- `<quote 3, source, link>`

## 5. What got roasted

The criticism worth taking seriously.

- `<critique 1 and the planned response>`
- `<critique 2 and the planned response>`
- `<a thing you'd remove or rename in v0.2>`

## 6. What to improve

- Copy that didn't land: `<which post, what you'd change>`
- Surface that was a waste: `<which channel, what to skip next time>`
- Automation that broke: `<which script, what failed>`
- Operational pain: `<the thing you wish you'd prepped>`

## 7. Next version

- `v0.2` scope (small, specific): `<bullet list>`
- Roadmap items pulled forward: `<list>`
- Roadmap items pushed back: `<list>`

## 8. Verbatim launch numbers (raw)

For later trend analysis.

```text
date,stars,clones,visitors,issues,prs
<launch-1d>,<n>,<n>,<n>,<n>,<n>
<launch+0>,<n>,<n>,<n>,<n>,<n>
<launch+1>,<n>,<n>,<n>,<n>,<n>
<launch+7>,<n>,<n>,<n>,<n>,<n>
```
